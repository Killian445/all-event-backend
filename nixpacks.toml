# Nixpacks (Railway) — Laravel dans ce dépôt.
# - Les extensions PHP sont surtout déclarées dans composer.json (ext-pdo_pgsql).
# - Document root pour Nginx / PHP built-in selon le provider.
# Référence : https://nixpacks.com/docs/providers/php

[variables]
NIXPACKS_PHP_ROOT_DIR = "/app/public"
